import { Cinzel, Creepster } from 'next/font/google'

export const cinzel = Cinzel({ subsets: ['latin'], variable: '--font-cinzel', display: 'swap' })
export const creepster = Creepster({ subsets: ['latin'], variable: '--font-creepster', display: 'swap', weight:"400"})